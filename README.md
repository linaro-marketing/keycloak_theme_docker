# Linaro Keycloak Theme

This repo contains the custom Linaro branded theme used in our sso service which used [keycloak](https://github.com/keycloak/keycloak).

## Usage`

Run the ./docker-run.sh file using:

```bash
./docker-run.sh
```
